const state = {
  data: {}
}

const mutations = {

  EDIT_ADDRESS (state, newAddress) {
    let address = state.data.find(address)
  },

  ADD_ADDRESS (state, newAddress) {
    
  },

  DELETE_ADDRESS (state, deletedAddress) {

  },


}

export default {
  state,
  mutations
}